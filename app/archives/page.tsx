import ArchivesClient from "./ArchivesClient";

export const metadata = {
  title: "Archives | Elkaza",
  description: "Browse site sections by tag and year.",
};

export default function ArchivesPage() {
  return <ArchivesClient />;
}
